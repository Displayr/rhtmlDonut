version: 1
tests:
  - type: single_page_one_example_per_config
    general_comments:
      - 'NB for these examples we set minAngle=0 to emphasize variations in labelMaxLineAngle'
    width: 350
    height: 350
    rowSize: 5
    data: data.decreasing_value_sets.100_decreasing_values
    comments:
      - location: 'labelMaxLineAngle=45'
        text: label overlap
      - location: 'labelMaxLineAngle=60'
        text: label overlap
    config:
      - '{ "settings" : { "minAngle": "0", "labelMaxLineAngle" : "30", "title" : "labelMaxLineAngle=30" } }'
      - '{ "settings" : { "minAngle": "0", "labelMaxLineAngle" : "45", "title" : "labelMaxLineAngle=45" } }'
      - '{ "settings" : { "minAngle": "0", "labelMaxLineAngle" : "60", "title" : "labelMaxLineAngle=60" } }'
      - '{ "settings" : { "minAngle": "0", "labelMaxLineAngle" : "75", "title" : "labelMaxLineAngle=75" } }'
      - '{ "settings" : { "minAngle": "0", "labelMaxLineAngle" : "90", "title" : "labelMaxLineAngle=90" } }'
