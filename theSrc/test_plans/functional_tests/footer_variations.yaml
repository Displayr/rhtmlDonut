version: 1
tests:
  - type: single_page_one_example_per_config
    width: 250
    height: 300
    rowSize: 3
    data: data.test_plan.abc_rbg
    config:
      - '{ "settings" : { "footer" : "default footer settings" } }'
      - '{ "settings" : { "footer" : "custom footer settings" } }|config.footer_12_georgia_lightblue'
      - '{ "settings" : { "footer" : "multiline<br>footers<br>are supported" } }'